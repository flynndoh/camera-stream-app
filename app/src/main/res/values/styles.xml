<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="Theme.CameraStreamer" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Secondary color used for accent elements -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Background and text colors -->
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>
    </style>

    <!-- Light and Dark Mode Color Definitions -->
    <color name="colorPrimary">#EF7E04</color>
    <color name="colorPrimaryVariant">#A85902</color>
    <color name="windowBackground">#FFFFFF</color>

</resources>
